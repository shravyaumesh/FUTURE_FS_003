/**
 * Optional helper: shows how to seed Firestore (instructions in README).
 * No-op on postinstall; included as reference.
 */
console.log('Local data available in /data. To use Firebase, follow README steps.');
